// libs
import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  item: {
    backgroundColor: '#eee',
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
    // borderRadius: 100,
  },
});
